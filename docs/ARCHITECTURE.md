# Architecture

The browser talks directly to the LocalizeOS Intelligent Contract on StudioNet. The contract owns projects, policy snapshots, cases, approved candidate indexes, semantic-memory pointers, and release commitments. Routine translation work is browser-local and becomes authoritative only after a bounded case submission and deterministic post-consensus transition.
