# Integration

Configure `NEXT_PUBLIC_LOCALIZEOS_CONTRACT` in the Vercel environment. The browser uses `genlayer-js@1.1.8`, `studionet`, and an injected EIP-1193 provider. Read failures are unavailable states. Writes are blocked unless the wallet reports chain `61999`, then wait for finality, inspect GenVM execution, and reread state.
