# Proof receipts

Verified files currently included:

- `deployment-receipt.json` — deployed address, transaction, source commit/hash and observed deployment result.
- `schema-receipt.json` — public methods and `resolve_case` write exposure.

No lifecycle receipts currently exist. Add project, policy, case, resolution, memory, supersession and release receipts only after signed transactions are finalized and reread from chain. Never add private keys, mnemonics, passwords, or private source material.
