import SiteHeader from "../../components/site-header";

export default function Queue() { return <main className="shell"><SiteHeader /><section className="workspace"><div className="eyebrow">STRING QUEUE · LIVE CONTRACT</div><h1>Nothing escalated yet.</h1><p className="lede">Routine work stays off-chain. Cases appear here only after a bounded artifact is submitted to StudioNet.</p><div className="empty-state"><strong>Live state unavailable</strong><p>Configure NEXT_PUBLIC_LOCALIZEOS_CONTRACT to read the authoritative queue.</p></div></section></main>; }
