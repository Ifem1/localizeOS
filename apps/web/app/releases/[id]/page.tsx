import SiteHeader from "../../../components/site-header";

export default async function ReleaseDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <main className="shell"><SiteHeader /><section className="workspace"><div className="eyebrow">RELEASE {id} · PRESS PROOF</div><h1>Locale release receipt</h1><p className="lede">A receipt is authoritative only after finalized GenVM execution and a fresh contract read.</p><div className="empty-state"><strong>Release receipt unavailable</strong><p>Configure the deployed contract to inspect the immutable manifest digest, policy version and resolved case IDs.</p></div></section></main>; }
