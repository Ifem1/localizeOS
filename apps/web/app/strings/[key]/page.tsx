import SiteHeader from "../../../components/site-header";

export default async function StringDetail({ params }: { params: Promise<{ key: string }> }) { const { key } = await params; return <main className="shell"><SiteHeader /><section className="workspace"><div className="eyebrow">STRING CONTEXT · {key}</div><h1>Context preview</h1><p className="lede">This screen is live-state-only. Draft edits stay in the browser until a bounded artifact is submitted.</p><div className="empty-state"><strong>String record unavailable</strong><p>Configure the deployed contract address and open a real queue record to inspect authoritative source, target, policy version and placeholder lint.</p></div></section></main>; }
