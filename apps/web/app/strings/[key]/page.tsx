import Link from "next/link";
export default async function StringDetail({ params }: { params: Promise<{ key: string }> }) { const { key } = await params; return <main className="shell"><header className="topbar"><Link className="wordmark" href="/">LOCALIZE<span>OS</span></Link><nav><Link href="/queue">Queue</Link><Link href="/policy">Policy</Link><Link href="/cases">Cases</Link><Link href="/releases">Releases</Link></nav><button className="wallet">Connect wallet</button></header><section className="workspace"><div className="eyebrow">STRING CONTEXT · {key}</div><h1>Context preview</h1><p className="lede">This screen is intentionally live-state-only. Draft edits stay in the browser until a bounded artifact is submitted.</p><div className="empty-state"><strong>String record unavailable</strong><p>Configure the deployed contract address and open a real queue record to inspect authoritative source, target, policy version and placeholder lint.</p></div></section></main>; }
