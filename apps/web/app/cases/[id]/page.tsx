import SiteHeader from "../../../components/site-header";

export default async function CaseDetail({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <main className="shell"><SiteHeader /><section className="workspace"><div className="eyebrow">CASE {id} · DISAGREEMENT DESK</div><h1>Resolution desk</h1><p className="lede">Candidates, policy evidence, and related records are shown here before a permissionless consensus resolution.</p><div className="empty-state"><strong>Case record unavailable</strong><p>No fabricated candidate or validator result is shown. Configure a deployed contract to load the authoritative case.</p></div></section></main>; }
